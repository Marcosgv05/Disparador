# ====== CONFIGURAÇÕES DA FERRAMENTA ======

# Delay entre mensagens (em milissegundos)
MESSAGE_DELAY=3000

# Delay entre números diferentes (em milissegundos)
NUMBER_DELAY=5000

# Modo de alternância: 'sequential' ou 'random'
ROTATION_MODE=sequential

# ====== CONFIGURAÇÕES DO SERVIDOR ======

# Porta do servidor (Railway/Render usam variável PORT automática)
PORT=3000

# URL base da aplicação (para deploy)
# Exemplo: https://seu-app.railway.app
BASE_URL=http://localhost:3000

# Ambiente (development ou production)
NODE_ENV=development

# ====== BANCO DE DADOS (OBRIGATÓRIO PARA PRODUÇÃO) ======

# URL de conexão PostgreSQL (Railway fornece automaticamente)
# Formato: postgresql://usuario:senha@host:porta/database
# No Railway: vai para Variables e copie DATABASE_URL do serviço PostgreSQL
# DATABASE_URL=postgresql://postgres:senha@host:5432/railway

# ====== SEGURANÇA (IMPORTANTE PARA PRODUÇÃO!) ======

# Secret para JWT (TROQUE em produção!)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=whatsapp-dispatcher-secret-key-change-in-production

# Secret para sessões (TROQUE em produção!)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=whatsapp-dispatcher-session-secret

# CORS Origins (separados por vírgula)
# Em produção: https://seu-app.railway.app,https://seu-dominio.com
# IMPORTANTE: Especifique TODOS os domínios permitidos explicitamente
CORS_ORIGIN=http://localhost:3000

# ====== FIREBASE (OBRIGATÓRIO PARA AUTENTICAÇÃO) ======

# Project ID do Firebase (obrigatório para autenticação)
# Pegue em: Firebase Console → Project Settings → General → Project ID
FIREBASE_PROJECT_ID=seu-projeto-firebase

# Credenciais do Firebase Admin (para produção)
# Opção 1: Arquivo de credenciais (não recomendado para Railway)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Opção 2: Credenciais inline (recomendado para Railway)
# Cole o conteúdo JSON do serviceAccountKey.json aqui (em uma linha)
# FIREBASE_ADMIN_CREDENTIALS={"type":"service_account","project_id":"..."}

# ====== ADMINISTRAÇÃO ======

# Emails de administradores (separados por vírgula)
# Esses usuários terão acesso ao painel de gestão em /admin.html
ADMIN_EMAILS=admin@seudominio.com
